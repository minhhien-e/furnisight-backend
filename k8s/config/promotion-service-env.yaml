apiVersion: v1
data:
  # --- CẤU HÌNH HỆ THỐNG (SYSTEM) ---
  # --- DATABASE ---
  SPRING_DATASOURCE_URL: jdbc:postgresql://postgres.furnisight-infras.svc.cluster.local:5432/furnisight_promotion_db
  SPRING_DATASOURCE_USERNAME: postgres

kind: ConfigMap
metadata:
  name: promotion-service-env
  namespace: furnisight-apps
